# Config de build de producción para Vite (NO secreto — solo una ruta relativa).
# El frontend llama a /api en el MISMO dominio; Firebase Hosting reescribe /api/** hacia
# el backend en Cloud Run (ver firebase.json). Así no hay CORS y el dominio puede cambiar
# sin recompilar.
VITE_API_URL=/api
